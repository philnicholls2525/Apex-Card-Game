{
  "name": "apex-football-cards",
  "version": "0.42.0-beta.1",
  "private": true,
  "type": "module",
  "description": "APEX vanilla browser game with a Supabase-authoritative beta backend",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check app.js && node --check src/main.js && node --check src/auth.js && node --check src/game-api.js && node --check src/state-sync.js",
    "serve": "npx serve .",
    "supabase:start": "supabase start",
    "supabase:reset": "supabase db reset",
    "supabase:test": "supabase test db",
    "supabase:functions": "supabase functions serve game-api"
  },
  "engines": {
    "node": ">=22"
  }
}

